# Snakemake working directory (conda envs, job metadata, logs)
.snakemake/

# Pipeline outputs (never commit results or raw reads)
results/
logs/
run.log
*.log
*.fastq
*.fastq.gz

# Claude Code / editor session files
.session-backup*
*.jsonl
RESUME_SESSION.md

# OS cruft
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Python
__pycache__/
*.pyc
*.pyo

# Derived index files (rebuilt by the pipeline)
*.ndb
*.nhr
*.nin
*.njs
*.nog
*.nos
*.not
*.nsq
*.ntf
*.nto
*.mmi
*.fai

# PubMLST snapshots are downloaded per scheme pack (tools/fetch_pubmlst.py),
# never committed: they go stale and PubMLST is the citable source.
schemes/*/pubmlst/
